services:
  - type: web
    name: trip-confirmation
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:web
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5
